dsn: 'mysql://hydra:hydrapass@tcp(mysql:3306)/hydra?parseTime=true'

urls:
  login: http://localhost/test/login
  consent: http://localhost/test/consent
  self:
    issuer: https://localhost:9000/

secrets:
  system:
    - this-is-the-primary-secret

oauth2:
  expose_internal_errors: true
  allowed_top_level_claims:
    - role

strategies:
  access_token: jwt